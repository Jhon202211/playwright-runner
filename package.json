{
  "name": "playwright-runner",
  "version": "1.0.0",
  "description": "Backend runner para ejecutar tests Playwright desde el panel Cloudflare",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx playwright install --with-deps"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "playwright": "^1.50.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  },
  "engines": {
    "node": ">=18"
  }
}
